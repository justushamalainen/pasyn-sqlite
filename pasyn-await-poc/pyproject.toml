[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pasyn-await-poc"
version = "0.1.0"
description = "Proof of concept for native Python awaitables in Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
